int i, j;

int main()
{
//    const a = 10;
    if (i <= 0x0) {
        printInt(20);
        printLn();
    }
    j = 16;
    while (j < 0x10) {
        printInt(30);
        printLn();
    }
    i = i - 1;
    i + j;
    if (i < j) {
        i = 1;
        printInt(i);
        printLn();
        printHex(j);
    }
    return 0;
}

int arr_a[(10 + 2 * 3 - (20 - 30) * (2 / 1) + 0) * (8 % 7) * 7];
